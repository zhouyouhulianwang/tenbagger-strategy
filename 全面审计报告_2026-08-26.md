# Tenbagger 量化交易系统全面审计报告

**日期**: 2026-08-26 ｜ **版本**: v9.3 (commit 67b91e7) ｜ **范围**: 框架 / 代码 / 逻辑 / 风控 / 日志 / 配置 / 部署，Alpaca paper 与 live 两条路径
**方法**: 全文通读 ~4500 行生产代码 + 服务器部署实地检查（systemd/cron/logrotate/密钥/磁盘/NTP/git）+ 历史事故与实验档案交叉验证
**分级**: P0 = 上实盘前必须解决 ｜ P1 = 重要缺陷，影响 paper 验证有效性或运维安全 ｜ P2 = 改进项

---

## 一、总体结论

这套系统的**风控骨架和防错设计是合格的**：三层组合风控（日亏 -3% / 回撤熔断 -10% / 硬止损 -8%）经过消融实验验证，失败默认方向全是"不动仓"（fail-safe），状态原子写 + 单实例锁 + 启动孤儿单对账 + PDT 保护 + kill switch + 每日/每周报告 + 数据新鲜度闸门，这些在同类个人量化系统里属于上游水平。

**但 paper 与 live 之间存在几个结构性落差**，集中在三点：
1. **数据质量**：免费 IEX 行情延迟 ~15 分钟，止损和 paper 成交都建立在它上面——paper 的收益是实盘的上限，不是预期值。
2. **止损是"轮询式"而非"挂单式"**：券商侧没有 resting stop 单，隔夜跳空时保护有 15 分钟以上的空窗（8/5 DVA -19% 事件已实证）。
3. **运维单点**：进程 hang（非崩溃）时 systemd 不会重启，Telegram 是唯一告警通道。

**当前 paper 可以继续跑；上实盘前 P0/P1 项必须清零。**

---

## 二、确认的具体缺陷（代码级）

### P1-1 假日周一无调仓兜底 —— 9 月 7 日就会踩中

`should_rebalance()` 只认 `weekday() == REBALANCE_WEEKDAY(0)`：

```python
today = now_et()
if today.weekday() != self.config.REBALANCE_WEEKDAY:
    return False
```

- 调仓被 `market_open` 门控（run_cycle 4140 行附近），假日周一市场不开 → 当天不调仓；周二 weekday=1 → 直接返回 False → **整周不调仓**。
- 2026-09-07 Labor Day 就是周一，距今不到两周。
- 回测引擎每 5 个交易日调一次（weekday-agnostic），live 遇假日周一整周跳空 → 回测/实盘节奏漂移，恰好这类周往往是波动周。
- **建议**：`last_rebalance` 距今 > 6 天且今天是交易日 → 允许周二（或当周第一个交易日）补调仓。

### P2-1 `get_bars_batch` docstring 失效
979 行 `self._last_bar_volumes = {}` 写在了 docstring 前面，文档字符串变成无效语句。无害但说明该函数近期改动未 review。

### P2-2 `should_rebalance` 裸 `except:`（3586 行附近）
吞掉所有异常包括 KeyboardInterrupt 语义类问题，建议收窄为 `except (json.JSONDecodeError, OSError, ValueError)`。

### P2-3 kill switch 激活日不发日报
kill switch 分支在 `run_cycle` 最前 return，规则#50 的日报钩子在其后 → 停交易的日子里报告也停了。用户最需要知道"今天为什么没动作"。建议：kill switch 分支也发日报（标注 KILL SWITCH 生效中）。

### 已修复（本周，67b91e7）
`equity_at_last_rebalance` 被 `_save_state` 每分钟覆写丢弃——周报/调仓报告的周收益基准会悄悄消失，与 v9.2 `last_rebalance` 丢失同一 bug 类型。

---

## 三、Alpaca paper 的缺陷（验证有效性）

| # | 缺陷 | 影响 | 级别 |
|---|---|---|---|
| P-1 | **IEX 免费行情延迟 ~15min**：`DATA_FEED='iex'`，positions 的 current_price、日线 bar、止损触发全部基于此 | 止损实际触发价比看到的价格晚 15 分钟；paper 日报的"当日收益"也含此噪声 | **P1**（paper 验证失真） |
| P-2 | **paper 成交模拟过于乐观**：Alpaca paper 市价单按理想价立即成交，无真实点差、无部分成交、无拒单 | paper 收益是实盘上限；点差成本（大盘 1.5-10bps 模型）在 paper 里不会真实发生 | P1（认知层面必须明确） |
| P-3 | 无成交价 vs 信号价的滑点追踪 | 无法量化 paper→live 的执行衰减 | P2（建议 trades.jsonl 加 `signal_close` 字段做月度 TCA） |
| P-4 | 止损依赖 60s 轮询 + 延迟行情，券商侧无 resting stop 单 | 隔夜跳空（如 8/5 DVA -19%）保护空窗 = 15min 延迟 + 最多 60s 轮询 + 市价单滑点 | **P1** |
| P-5 | 调仓集中在周一 10:00 一个时点，全仓轮换 ~2 分钟内市价单完成 | 时点风险；好在避开了 9:30 开盘噪音（TRADING_WINDOW 设计正确） | P2 |

**做对了的**：`tif='day'` + 仅盘中交易（无 extended hours 单）、卖→买资金链（现金不足自动 scale，8/24 实测 x0.93 正常）、PDT 保护、client_order_id 前缀幂等（同一提交内重试复用同一 ID，Alpaca 会去重）、启动时孤儿单对账、双实例锁。

---

## 四、Live 实盘的缺陷（上线前清单）

| # | 缺陷 | 后果 | 级别 |
|---|---|---|---|
| L-1 | **必须订阅 SIP 行情**（Altcar/Algo Trader Plus ~$9/月）并把 `DATA_FEED='sip'` | 否则 L-2 到 L-4 全部建立在 15 分钟延迟数据上 | **P0** |
| L-2 | **账户类型确认**：卖后买资金链假设卖出资金立即可用。margin 账户 OK；现金账户 T+1 结算会导致调仓买入大面积 scale 甚至 good-faith violation | 调仓失败/半轮换 | **P0**（确认即可） |
| L-3 | 凭证轮换未执行（GitHub token / Alpaca keys / SSH 密码曾出现在沙箱会话中） | 泄露面 | **P0**（用户侧动作） |
| L-4 | `INITIAL_CAPITAL=100k` 硬编码为累计收益基准 | live 出入金后"累计收益"失真；paper 无出入金故无影响 | P1 |
| L-5 | LIVE_TRADING_ACK 双重确认、paper/live URL 环境隔离已有 ✓；但 live 切换只需改 env，无"灰度"步骤 | 建议 live 首月用小额（如 $5-10k）+ 每日人工核对成交 | P1（流程建议） |
| L-6 | 止损市价单在 live 的真实滑点会显著大于 paper | -8% 硬止损实际成交可能在 -9%~-10% | P1（认知+监控） |

---

## 五、风控体系评估

**结构合理的部分（不建议动）**：
- 日亏 -3%：7 阈值扫描的尖锐全局最优（2%→280/1.23，3.5%→135/0.79），A+ 头号防线
- 回撤 -10% + 24h 冷却 + scheduled 重建：消融值 +54pp，空仓窗保护性已实证
- 硬止损 -8%：消融值 +174pp；trailing 50/100 六年零触发（冗余但保留作安全网，正确决策）
- 单票 30% 上限 + 6 票 → 单票极端损失对组合冲击 ≤ 2.4%，快于日亏防线，自洽
- 失败默认方向全部 fail-safe：账户读失败→HOLD、数据不新鲜→中止、持仓读失败→保持快照

**缺陷**：

| # | 缺陷 | 级别 |
|---|---|---|
| R-1 | **风控全部依赖本进程存活**：进程 hang（网络死锁等，非崩溃）→ systemd 不重启、止损停火、无告警。multifactor 系统已有 watchdog.sh 范例，tenbagger 没有 | **P1** |
| R-2 | Telegram 是唯一告警通道，token 失效 = 全静默。缓解：规则#50 日报本身就是心跳（**收不到 16:05 ET 日报 = 系统异常**，建议用户以此为准）；仍建议 watchdog 独立告警 | P1 |
| R-3 | `_last_equity` 未持久化：跨午夜重启时当日基线重置为重启时净值（≈昨收），影响微小 | P2 |
| R-4 | 冷却期用墙钟 24h 跨周末自然消化，可接受 | 无需改 |

---

## 六、日志 / 状态 / 配置文件

**做对了的**：
- logrotate `copytruncate`（与 systemd `append:` fd 持有模式匹配，配置正确）✓ weekly × 12 份 ≈ 3 个月回溯
- state.json 原子写（tmp+rename）+ 每日 cron 备份保留 30 天 ✓
- trades.jsonl 结构化成交日志（含成本分解）✓
- 全链路 ET 时间（ETFormatter），服务器 Asia/Shanghai + NTP 同步正常 ✓

**缺陷**：

| # | 缺陷 | 级别 |
|---|---|---|
| C-1 | **`Config.load_from_file()` 存在但 `main()` 从不调用**——config.json 支持是死代码，所有参数变更必须改源码 + git commit。好处是参数变更天然进版本史（本项目一直这么做的）；风险是无法运维侧热调 | P2（明确取舍即可） |
| C-2 | Universe 硬编码 2026 年成分股（S&P500+NDX），无自动刷新机制。成分股季度调整 → 名单逐步陈旧（live 无幸存者偏差问题，但会漏掉新龙头） | P2（建议每季度人工刷新名单，流程写入 README） |
| C-3 | cron 按北京时间排程（该服务器 cron 守护忽略 CRON_TZ，multifactor 注释已实证）。tenbagger 的 PIT 重建 `0 10 * * 6` = 周五 22:00 ET，当前可用；**夏/冬令时切换周需复核** | P2 |
| C-4 | 磁盘 64%（60G/98G），日志增长可控，暂无风险 | 观察项 |
| C-5 | 密钥文件 `/root/.tenbagger-env` 权限 600 root ✓；但沙箱侧 `.secrets/` 明文持久化 + git push 命令行带 token（进程列表瞬态可见） | P2 |

---

## 七、回测 ↔ 实盘 parity（对 paper 结论可信度的影响）

| 项 | 状态 |
|---|---|
| 仓位 sizing（vol×score + 30%/5% + 25% 漂移带） | ✓ v9.3 已对齐 |
| 基本面数据源（PIT SEC XBRL 同源） | ✓ 已对齐 |
| 调仓节奏 | ⚠️ 回测每 5 交易日 vs live 每周一 + **假日周一跳空**（P1-1） |
| 执行价 | ⚠️ 回测用 t+1 收盘代理次日开盘；live 周一 10:00 市价单——方向一致，幅度有差 |
| 成交量口径 | ⚠️ 回测 consolidated vs live IEX（~2-3%），流动性过滤器的阈值不可直接平移（代码注释已标注） |
| 幸存者偏差 | ⚠️ 回测池为 2026 年成分股，数字偏高估（RESULTS.md 已声明） |

---

## 八、行动清单（按优先级）

**P0 — 上实盘前必须完成**：
1. 订阅 SIP 行情，`DATA_FEED='sip'`，paper 也切过去重新验证至少 4 周
2. 确认 Alpaca 账户为 margin 类型（卖后买资金链）
3. 轮换全部凭证（GitHub token / Alpaca keys / SSH 密码）

**P1 — 建议本周处理**：
4. 调仓假日兜底：`last_rebalance` 超 6 天 + 当日开市 → 当周首个交易日补调仓（**9/7 Labor Day 前必须上线**）
5. tenbagger watchdog cron：检查 `systemctl is-active` + monitor.out 最后周期时间戳（盘中 >5 分钟无周期 = hang），异常 Telegram 告警
6. kill switch 日也发日报（标注停交易状态）
7. live 灰度计划：首月小额 + 每日人工核对

**P2 — 排期改进**：
8. trades.jsonl 加信号价字段，建立月度滑点 TCA
9. 止损挂单化评估（GTC stop 单挂券商侧，与调仓/风控的协调需设计）
10. `get_bars_batch` docstring 修复、裸 except 收窄
11. Universe 季度刷新流程写入 README
12. INITIAL_CAPITAL 动态化（live 出入金自适应）

---

*审计依据：tenbagger_v8_2_production.py @ 67b91e7 全文、服务器 47.88.59.160 实地配置、data/ 下全部实验 JSON、8/5 DVA 与 8/24 调仓实盘日志。*
